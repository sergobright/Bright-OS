apply plugin: 'com.android.application'

def androidKeystorePath = System.getenv('BRIGHT_OS_ANDROID_KEYSTORE_PATH')
def androidStorePassword = System.getenv('BRIGHT_OS_ANDROID_STORE_PASSWORD')
def androidKeyAlias = System.getenv('BRIGHT_OS_ANDROID_KEY_ALIAS')
def androidKeyPassword = System.getenv('BRIGHT_OS_ANDROID_KEY_PASSWORD') ?: androidStorePassword
def androidReleaseKeystore = file(androidKeystorePath ?: 'missing-release-keystore.jks')
def brightOsWebVersion = System.getenv('BRIGHT_OS_APP_VERSION') ?: '0.0.10.1'
def brightOsProductionFallbackBundleVersion = System.getenv('BRIGHT_OS_PROD_FALLBACK_BUNDLE_VERSION') ?: '0.0.10.1'
def brightOsNonProductionFallbackBundleVersion = System.getenv('BRIGHT_OS_NON_PROD_FALLBACK_BUNDLE_VERSION') ?: "${brightOsWebVersion}.0"

android {
    namespace = "world.brightos.bright_os_client"
    compileSdk = rootProject.ext.compileSdkVersion
    defaultConfig {
        applicationId "world.brightos.bright_os_client"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "0.0.10.1"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        manifestPlaceholders = [brightAppLabel: 'Bright OS']
        aaptOptions {
             // Files and dirs to omit from the packaged assets dir, modified to accommodate modern web apps.
             // Default: https://android.googlesource.com/platform/frameworks/base/+/282e181b58cf72b6ca770dc7ca5f91f135444502/tools/aapt/AaptAssets.cpp#61
            ignoreAssetsPattern = '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
        }
    }
    buildFeatures {
        buildConfig true
    }
    flavorDimensions "environment"
    productFlavors {
        production {
            dimension "environment"
            manifestPlaceholders = [brightAppLabel: 'Bright OS']
            buildConfigField "String", "BRIGHT_OS_APP_BUILD", '"1"'
            buildConfigField "String", "BRIGHT_OS_FALLBACK_BUNDLE_VERSION", "\"${brightOsProductionFallbackBundleVersion}\""
            buildConfigField "String", "BRIGHT_OS_OTA_MANIFEST_URL", '"https://app.brightos.world/mobile-update/manifest.json"'
            buildConfigField "String", "BRIGHT_OS_ENVIRONMENT", '"prod"'
            buildConfigField "String", "BRIGHT_OS_PREVIEW_SLOT", '""'
            buildConfigField "String", "BRIGHT_OS_OTA_CHANNEL", '"app.brightos.world/mobile-update"'
            buildConfigField "String", "BRIGHT_OS_APP_LABEL", '"Bright OS"'
        }
        dev {
            dimension "environment"
            applicationIdSuffix ".dev"
            manifestPlaceholders = [brightAppLabel: 'Bright OS Dev']
            buildConfigField "String", "BRIGHT_OS_APP_BUILD", '"1-dev"'
            buildConfigField "String", "BRIGHT_OS_FALLBACK_BUNDLE_VERSION", "\"${brightOsNonProductionFallbackBundleVersion}\""
            buildConfigField "String", "BRIGHT_OS_OTA_MANIFEST_URL", '"https://dev.brightos.world/mobile-update/manifest.json"'
            buildConfigField "String", "BRIGHT_OS_ENVIRONMENT", '"dev"'
            buildConfigField "String", "BRIGHT_OS_PREVIEW_SLOT", '""'
            buildConfigField "String", "BRIGHT_OS_OTA_CHANNEL", '"dev.brightos.world/mobile-update"'
            buildConfigField "String", "BRIGHT_OS_APP_LABEL", '"Bright OS Dev"'
        }
        previewA {
            dimension "environment"
            applicationIdSuffix ".preview.a"
            manifestPlaceholders = [brightAppLabel: 'Bright OS A']
            buildConfigField "String", "BRIGHT_OS_APP_BUILD", '"1-a"'
            buildConfigField "String", "BRIGHT_OS_FALLBACK_BUNDLE_VERSION", "\"${brightOsNonProductionFallbackBundleVersion}\""
            buildConfigField "String", "BRIGHT_OS_OTA_MANIFEST_URL", '"https://a.test.brightos.world/mobile-update/manifest.json"'
            buildConfigField "String", "BRIGHT_OS_ENVIRONMENT", '"preview-a"'
            buildConfigField "String", "BRIGHT_OS_PREVIEW_SLOT", '"A"'
            buildConfigField "String", "BRIGHT_OS_OTA_CHANNEL", '"a.test.brightos.world/mobile-update"'
            buildConfigField "String", "BRIGHT_OS_APP_LABEL", '"Bright OS A"'
        }
        previewB {
            dimension "environment"
            applicationIdSuffix ".preview.b"
            manifestPlaceholders = [brightAppLabel: 'Bright OS B']
            buildConfigField "String", "BRIGHT_OS_APP_BUILD", '"1-b"'
            buildConfigField "String", "BRIGHT_OS_FALLBACK_BUNDLE_VERSION", "\"${brightOsNonProductionFallbackBundleVersion}\""
            buildConfigField "String", "BRIGHT_OS_OTA_MANIFEST_URL", '"https://b.test.brightos.world/mobile-update/manifest.json"'
            buildConfigField "String", "BRIGHT_OS_ENVIRONMENT", '"preview-b"'
            buildConfigField "String", "BRIGHT_OS_PREVIEW_SLOT", '"B"'
            buildConfigField "String", "BRIGHT_OS_OTA_CHANNEL", '"b.test.brightos.world/mobile-update"'
            buildConfigField "String", "BRIGHT_OS_APP_LABEL", '"Bright OS B"'
        }
        previewC {
            dimension "environment"
            applicationIdSuffix ".preview.c"
            manifestPlaceholders = [brightAppLabel: 'Bright OS C']
            buildConfigField "String", "BRIGHT_OS_APP_BUILD", '"1-c"'
            buildConfigField "String", "BRIGHT_OS_FALLBACK_BUNDLE_VERSION", "\"${brightOsNonProductionFallbackBundleVersion}\""
            buildConfigField "String", "BRIGHT_OS_OTA_MANIFEST_URL", '"https://c.test.brightos.world/mobile-update/manifest.json"'
            buildConfigField "String", "BRIGHT_OS_ENVIRONMENT", '"preview-c"'
            buildConfigField "String", "BRIGHT_OS_PREVIEW_SLOT", '"C"'
            buildConfigField "String", "BRIGHT_OS_OTA_CHANNEL", '"c.test.brightos.world/mobile-update"'
            buildConfigField "String", "BRIGHT_OS_APP_LABEL", '"Bright OS C"'
        }
        previewD {
            dimension "environment"
            applicationIdSuffix ".preview.d"
            manifestPlaceholders = [brightAppLabel: 'Bright OS D']
            buildConfigField "String", "BRIGHT_OS_APP_BUILD", '"1-d"'
            buildConfigField "String", "BRIGHT_OS_FALLBACK_BUNDLE_VERSION", "\"${brightOsNonProductionFallbackBundleVersion}\""
            buildConfigField "String", "BRIGHT_OS_OTA_MANIFEST_URL", '"https://d.test.brightos.world/mobile-update/manifest.json"'
            buildConfigField "String", "BRIGHT_OS_ENVIRONMENT", '"preview-d"'
            buildConfigField "String", "BRIGHT_OS_PREVIEW_SLOT", '"D"'
            buildConfigField "String", "BRIGHT_OS_OTA_CHANNEL", '"d.test.brightos.world/mobile-update"'
            buildConfigField "String", "BRIGHT_OS_APP_LABEL", '"Bright OS D"'
        }
        previewE {
            dimension "environment"
            applicationIdSuffix ".preview.e"
            manifestPlaceholders = [brightAppLabel: 'Bright OS E']
            buildConfigField "String", "BRIGHT_OS_APP_BUILD", '"1-e"'
            buildConfigField "String", "BRIGHT_OS_FALLBACK_BUNDLE_VERSION", "\"${brightOsNonProductionFallbackBundleVersion}\""
            buildConfigField "String", "BRIGHT_OS_OTA_MANIFEST_URL", '"https://e.test.brightos.world/mobile-update/manifest.json"'
            buildConfigField "String", "BRIGHT_OS_ENVIRONMENT", '"preview-e"'
            buildConfigField "String", "BRIGHT_OS_PREVIEW_SLOT", '"E"'
            buildConfigField "String", "BRIGHT_OS_OTA_CHANNEL", '"e.test.brightos.world/mobile-update"'
            buildConfigField "String", "BRIGHT_OS_APP_LABEL", '"Bright OS E"'
        }
    }
    signingConfigs {
        releaseEnv {
            storeFile androidReleaseKeystore
            storePassword androidStorePassword ?: ''
            keyAlias androidKeyAlias ?: ''
            keyPassword androidKeyPassword ?: ''
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.releaseEnv
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

gradle.taskGraph.whenReady { graph ->
    def needsReleaseSigning = graph.allTasks.any { task ->
        def taskName = task.name.toLowerCase()
        task.path.startsWith(':app:')
            && taskName.contains('release')
            && (taskName.startsWith('assemble')
                || taskName.startsWith('bundle')
                || taskName.startsWith('package')
                || taskName.startsWith('sign')
                || taskName.startsWith('validatesigning'))
    }
    if (needsReleaseSigning) {
        if (!androidKeystorePath || !androidStorePassword || !androidKeyAlias || !androidKeyPassword) {
            throw new GradleException("Release APK signing requires BRIGHT_OS_ANDROID_KEYSTORE_PATH, BRIGHT_OS_ANDROID_STORE_PASSWORD, BRIGHT_OS_ANDROID_KEY_ALIAS, and BRIGHT_OS_ANDROID_KEY_PASSWORD.")
        }
        if (!androidReleaseKeystore.exists()) {
            throw new GradleException("Release APK signing keystore does not exist: ${androidReleaseKeystore}")
        }
    }
}

repositories {
    flatDir{
        dirs '../capacitor-cordova-android-plugins/src/main/libs', 'libs'
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
    implementation project(':capacitor-android')
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-cordova-android-plugins')
}

apply from: 'capacitor.build.gradle'

try {
    def servicesJSON = file('google-services.json')
    if (servicesJSON.text) {
        apply plugin: 'com.google.gms.google-services'
    }
} catch(Exception e) {
    logger.info("google-services.json not found, google-services plugin not applied. Push Notifications won't work")
}
